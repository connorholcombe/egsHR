function scrollContact() {
const section = document.getElementById("scrollContact");
section.scrollIntoView({
    behavior: "smooth",
    block: "start",
    inline: "nearest",
    duration: 500
});
}

function scrollAbout() {
const section = document.getElementById("scrollAbout");
section.scrollIntoView({
    behavior: "smooth",
    block: "start",
    inline: "nearest",
    duration: 500
});
}

function scrollTop() {
const section = document.getElementById("scrollTop");
section.scrollIntoView({
    behavior: "smooth",
    block: "start",
    inline: "nearest",
    duration: 500
});
}